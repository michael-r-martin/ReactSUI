import React from "react";

// Spacer is a view that takes up space but doesn't render anything. It should expand to fill the available space.
const Spacer = () => {
    return (
        <div style={{ flex: 1 }}></div>
    )
};

export default Spacer;
